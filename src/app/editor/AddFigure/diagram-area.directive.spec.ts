import { DiagramAreaDirective } from './diagram-area.directive';

describe('DiagramAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new DiagramAreaDirective();
    expect(directive).toBeTruthy();
  });
});
