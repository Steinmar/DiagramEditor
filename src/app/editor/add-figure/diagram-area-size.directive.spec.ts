import { DiagramAreaSizeDirective } from './diagram-area-size.directive';

describe('DiagramAreaSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new DiagramAreaSizeDirective();
    expect(directive).toBeTruthy();
  });
});
